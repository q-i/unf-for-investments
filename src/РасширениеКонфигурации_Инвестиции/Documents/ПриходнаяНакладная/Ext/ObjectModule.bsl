
&После("ПередЗаписью")
Процедура Инв_ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если ЭтоНовый() Тогда
		Инв_ОбщегоНазначения.УстановитьВремяДокумента(ЭтотОбъект);
	КонецЕсли;
	
КонецПроцедуры
